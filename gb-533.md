# gb-533 工业机器人安全包络校验

请生成一个名为 `robot-cell-safety-envelope-validator` 的 Go 全栈 Web 项目，面向工业机器人集成商与工厂安全工程师，用于离线描述机器人工作单元、维护安全区域、导入运动程序轨迹并仿真校验空间包络和联锁顺序。系统仅做决策支持与仿真，不得连接控制器、下发程序或直控机器人；不得实现商城、电商、预约、订单、仓储库存、财务、客服工单、博客或大众社区。

## 1. 项目概述与复杂度下限

- 类型：前后端分离的 B2B 全栈 Web 应用。
- 必须实现 4 个核心实体、5 个核心页面、至少 3 个共享组件、至少 2 个 hooks、至少 4 个后端中间件和 2 组前后端一致的共享枚举。
- 每个实体必须贯穿 PostgreSQL 表、Go model/dto/repository/service/handler/router、Angular type/api/store/page。
- 本项目用于验证跨文件协同改动能力。严禁合并职责到单一文件；任一实体字段、状态或权限调整应触达至少 5 个文件。

## 2. 核心实体

1. `RobotCell`（机器人工作单元）
   - 字段：`id`、`cell_code`、`name`、`layout_geojson`、`robot_model`、`controller_model`、`max_reach_mm`、`owner_team`、`cell_state`、`created_at`、`updated_at`。
   - 关系与动作：包含多个安全区与运动程序；支持建档、更新二维布局、冻结布局版本、停用。
   - 必须贯穿 `robot_cells` 表 -> `model/robot_cell.go` -> `dto/robot_cell.go` -> `repository/robot_cell_repository.go` -> `service/robot_cell_service.go` -> `handler/robot_cell_handler.go` -> `router/robot_cell_router.go` -> `frontend/src/types/robot-cell.ts` -> `api/robot-cell.ts` -> `stores/robot-cell.store.ts` -> 工作单元页面。
2. `SafetyZone`（安全区域）
   - 字段：`id`、`robot_cell_id`、`name`、`zone_type`、`polygon_geojson`、`min_height_mm`、`max_height_mm`、`speed_limit_mm_s`、`access_rule`、`zone_state`、`version`。
   - 关系与动作：隶属工作单元并被校验运行快照引用；支持绘制、修订、启用、停用和版本对比。
   - 必须贯穿 `safety_zones` 表及独立 model/dto/repository/service/handler/router/type/api/store/page 文件。
3. `MotionProgram`（运动程序）
   - 字段：`id`、`robot_cell_id`、`program_code`、`version`、`trajectory_json`、`tool_radius_mm`、`payload_radius_mm`、`interlock_sequence_json`、`source_checksum`、`program_state`、`uploaded_by`、`uploaded_at`。
   - 关系与动作：隶属工作单元并拥有多次校验运行；支持导入结构化轨迹、解析、标记就绪、激活或被新版本替代。
   - 必须贯穿 `motion_programs` 表及独立后端分层、前端 type/api/store/page 文件。
4. `ValidationRun`（校验运行）
   - 字段：`id`、`motion_program_id`、`zone_snapshot`、`algorithm_version`、`input_hash`、`collision_events_json`、`interlock_findings_json`、`risk_score`、`validation_status`、`explanation`、`started_at`、`finished_at`、`reviewed_by`。
   - 关系与动作：冻结程序和安全区版本，保存可重放结果；支持排队、仿真、评审、接受和作废，不得覆盖历史。
   - 必须贯穿 `validation_runs` 表及独立后端分层、前端 type/api/store/page 文件。

## 3. 差异化算法与状态流

- 将每段轨迹按工具与负载半径扩张为扫掠包络，和安全区二维多边形及高度区间求交，输出首次相交时刻、轨迹段、区域、允许速度与实际速度。
- 把门禁、光栅、急停、速度切换等事件构成有向依赖序列，检测缺失前置、顺序反转和循环依赖。结果必须包含逐条证据，不得只给一个风险分。
- `MotionProgram`：`uploaded -> parsed -> ready -> active`，`active -> superseded`，解析失败到 `rejected`；非法迁移返回 409。
- `ValidationRun`：`queued -> simulating -> passed/failed -> reviewed -> accepted/voided`；同一 `input_hash + algorithm_version` 幂等，运行失败可重试但保留尝试记录。
- 仅支持约定的二维加高度近似，README 必须写明几何假设与误差边界；结果不能替代认证安全软件或人工验收，禁止下发控制命令。

## 4. 核心页面与实体消费

1. `/cells` 工作单元：消费 `RobotCell + SafetyZone`，展示布局、状态和区域摘要。
2. `/zones` 区域编辑器：消费 `SafetyZone + RobotCell`，绘制多边形、高度和速度限制并做几何校验。
3. `/programs` 程序版本：消费 `MotionProgram + RobotCell`，导入轨迹、查看 checksum 和版本状态。
4. `/validation` 仿真校验：消费 `ValidationRun + MotionProgram + SafetyZone`，回放轨迹、定位碰撞和联锁发现。
5. `/audit` 审计中心：消费四实体变更投影和审计日志，按操作者、request ID、实体与时间筛选。

`CellStateBadge` 被工作单元和程序页共用；`SafetyCanvas` 被区域和校验页共用；`FindingDrawer` 被校验、程序和审计页共用。

## 5. 横切关注点

- JWT + RBAC：至少包含 `admin`、`robot_programmer`、`safety_engineer`、`reviewer`、`auditor`；角色表、JWT claims、Go 中间件、路由、Angular guard、按钮显隐和类型联动。程序上传者不能接受自己的校验结果。
- 审计与追踪：所有写操作保存操作者、request ID、前后快照；校验运行保存输入哈希、算法版本、耗时和发现摘要。
- 全局错误：统一错误码；无效几何/轨迹返回 422，状态或版本冲突返回 409，权限问题返回 403，前端统一拦截展示。
- 限流与幂等：登录、轨迹导入和仿真接口分别限流；仿真使用 `Idempotency-Key` 防止重复运行。

## 6. 共享枚举、组件、hooks 与中间件

- `ZoneType = operating | restricted | service | escape`：后端 `backend/internal/constants/zone_type.go`，前端 `frontend/src/types/enums/zone-type.ts`。
- `ValidationStatus = queued | simulating | passed | failed | reviewed | accepted | voided`：后端 `backend/internal/constants/validation_status.go`，前端 `frontend/src/types/enums/validation-status.ts`。
- README 列出枚举在 model、dto、service 状态机、Angular store、组件、页面和测试中的全部位置。
- 共享组件：`CellStateBadge`、`SafetyCanvas`、`FindingDrawer`；Angular 可复用 inject helpers：`useAuth`、`useValidationRun`，放入 `frontend/src/hooks/`。
- 后端至少拆分 `request_id.go`、`recovery.go`、`auth.go`、`rbac.go`、`audit.go`、`error_handler.go` 六个中间件。

## 7. 技术、规模与文件结构

- 英文短名：`robot-cell-safety-envelope-validator`。
- 前端：Angular 17 + TypeScript + Vite + Angular Material + Signals store；后端：Go 1.22 + Gin + GORM；正式 PostgreSQL 16，runtime smoke 支持 SQLite；JWT。
- API 统一使用 `/api/v1`，另提供真实 `/healthz`；使用 validator/v10、统一响应和错误码、`%w` 错误链、request ID、结构化日志、构造器注入并实现优雅停机。多表写入用事务，状态迁移用条件更新，唯一性/幂等不能只靠前端。禁止 TODO、placeholder、空函数、硬编码成功响应和 handler 直连数据库。
- Go 功能代码 2500-4200 行、24-42 个功能 `.go` 文件且少于 5000 行，不计 `_test.go`、生成代码和 vendor；算法、状态机、权限必须有测试。
- 只保留普通项目单测；不得创建名称含 record/regression/grading/scoring 的评分测试，不得加入答案、`BUG_REPRO.md` 或标注流水线内部文件。
- 根 `go.work` 使用 `./backend`，支持根执行 `go work sync`、`go build ./backend/...`、`go test ./backend/...`。

```text
.
├── go.work  runtime_smoke.json
├── docker-compose.yml  .env  .env.example  .gitignore  README.md
├── output/
│   └── execution.md
├── backend/
│   ├── go.mod  Dockerfile  cmd/server/main.go
│   └── internal/
│       ├── config/ constants/ dto/ model/
│       ├── repository/ service/ handler/ router/
│       ├── middleware/ algorithm/ geometry/ util/
├── frontend/
│   ├── package.json  Dockerfile  nginx.conf
│   └── src/
│       ├── api/ stores/ types/ components/common/
│       ├── hooks/ pages/ router/ utils/
└── database/init.sql
```

四实体必须在所有后端层与前端 api/store/type 中按实体独立分文件。严禁合并职责到单一文件，不得用单个通用 CRUD handler/store 承载全部实体。

## 8. 运行 Manifest、部署与验证交付

- 固定端口：前端 `18533`、后端 `19533`、PostgreSQL 宿主机 `57533`；容器内为 `80/8080/5432`。
- 根 `runtime_smoke.json` 的唯一用途是提供可解析的启动 manifest，内容必须严格等价于：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20533",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "JWT_SECRET": "runtime-smoke-only-change-me"
  },
  "ready_url": "http://127.0.0.1:20533/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

- 后端必须支持 SQLite smoke 配置并自动建表/种子数据；正式 Compose 仍使用 PostgreSQL。manifest 禁止出现 `passed`、截图或执行证据字段。
- `docker-compose.yml` 顶层 `name: robot-cell-safety-envelope-validator` 且无 `version:`；编排 `db/backend/frontend`，使用命名卷和英文短名容器名前缀，数据库、后端和前端均有 healthcheck，并按健康条件依赖。
- `.env` 与 `.env.example` 均含 `COMPOSE_PROJECT_NAME=robot-cell-safety-envelope-validator`、`FRONTEND_PORT=18533`、`BACKEND_PORT=19533`、`DB_PORT=57533`、`DB_DRIVER=postgres`、数据库变量和 `JWT_SECRET`；`.env` 不提交。前端只访问 `/api`，Nginx 原样转发 `/api/v1` 到 `backend:8080`，另提供 `/api/healthz` 并支持 SPA。
- README 首屏给出 `docker compose up -d`，包含项目介绍、主要功能、角色账号、架构与目录、API 清单、技术栈表、环境变量与端口、本地开发、Docker 部署、枚举位置、算法假设与安全边界、runtime smoke、构建测试、常见问题、停止方式和 License；README 不得承载执行证据。
- 真实执行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`、`npm --prefix frontend ci`、`npm --prefix frontend run build`、`docker compose config --quiet`、`docker compose up -d --build`、`docker compose ps`；前后端均须有 healthcheck，所有长驻服务必须 healthy。
- 至少 8 个真实 API 冒烟：health、登录、四实体列表、工作单元/区域创建、程序导入、合法与非法状态迁移、仿真运行/结果、403 权限；同时覆盖成功和失败分支。
- API 冒烟还必须包含详情读取、未认证 401 与越权 403；四个核心实体各至少经过一条真实后端链路，五个页面都消费真实 `/api/v1`，不得使用静态 mock。并在 `backend/` 内复跑 `go build ./...`、`go vet ./...`、`go test ./...`。
- 仅使用 Codex 内置 Browser 验证登录、工作单元、区域、程序、校验五页交互，禁止外部 Chrome；实际完成创建或编辑、列表刷新和一次状态变化，并检查 console/network、文字溢出和遮挡。
- 所有构建、测试、runtime smoke、Compose、至少 8 条 API、Browser、console/network 和关闭证据只能写入 `output/execution.md`；截图必须直接放 `output/`，至少桌面和移动端各一张并由 execution.md 引用，禁止在其他文件复制证据或伪造结果。
- `output/execution.md` 必须包含项目编号、验证时间、实际端口、规模实测、根与 backend 构建/vet/test、前端构建、服务 healthy、API 表、Browser 操作步骤、截图相对路径、逐条需求覆盖、修复摘要、停服无残留和实现提交 hash。
- 完成后执行 `docker compose down -v --remove-orphans`，确认容器、网络和命名卷均已清理，并停止 runtime smoke 进程。
- 项目必须独立 `git init`，设置本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。真实实现与验证完成后提交 `git add -A && git commit -m "feat: implement and verify robot-cell-safety-envelope-validator"`，把实现提交 hash 写入 `output/execution.md`，再执行 `git add output/execution.md && git commit -m "docs: record verified execution"`，形成两次提交。`.gitignore` 排除 `.env`、依赖、构建产物、日志和本地 SQLite 文件，但保留 execution.md 与截图；最终 `git status --short` 为空，不得共用 Git 仓库。
